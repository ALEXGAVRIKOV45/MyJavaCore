package zooClasses;

import abstractClass.Animal;
import supportClasses.AnimalStatus;

public class Wolf extends Animal {

    public Wolf(String name, int age, AnimalStatus species) {
        super(name, age, species);
    }
}
