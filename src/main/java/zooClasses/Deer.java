package zooClasses;

import abstractClass.Animal;
import supportClasses.AnimalStatus;

public class Deer extends Animal {
    public Deer(String name, int age, AnimalStatus species) {
        super(name, age, species);
    }
}
